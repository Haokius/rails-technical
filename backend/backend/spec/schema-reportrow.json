{
  "type": "object",
  "properties": {
    "date": {
      "type": "string",
      "format": "date"
    },
    "ticker": {
      "type": "string"
    },
    "metric": {
      "type": "string",
      "enum": ["open", "high", "low", "close", "volume"]
    },
    "value": {
      "type": "number"
    }
  },
  "required": ["date", "ticker", "metric", "value"]
}
